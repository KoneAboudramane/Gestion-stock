# Utilisé uniquement par "npm run build" (mode production) — le portail est
# alors servi par Django lui-même (voir config/urls.py), donc l'API est sur la
# même origine : chemin relatif, pas besoin d'adresse absolue ni de CORS.
VITE_API_BASE_URL=/api
